---
# Rule Configuration
# Defines which rules are included in which commands
#
# Standard Rules: Located in standard/ (always updated by install script)
# Custom Rules: Located in custom/ (never touched by install script)
#
# Commands: Assembled from workflow/ and core/ rules
# Skills: Auto-generated from extended/ rules (one file = one skill)

commands:
  plan:
    description: Create structured implementation plans
    model: opus
    inject_skills: true
    rules:
      standard:
        - plan
        - git-operations
        - mcp-tools
        - project-conventions
        - context-management
      custom: []
  implement:
    description: Execute implementation plans in batches
    model: sonnet
    inject_skills: true
    rules:
      standard:
        - implement
        - tdd-enforcement
        - git-operations
        - mcp-tools
        - coding-standards
        - project-conventions
        - error-handling
        - validation
        - execution-verification
        - testing-strategies
        - context-management
      custom: []
  verify:
    description: Run tests and fix issues end-to-end
    model: sonnet
    inject_skills: true
    rules:
      standard:
        - verify
        - testing-strategies
        - git-operations
        - mcp-tools
        - execution-verification
        - coding-standards
        - error-handling
        - validation
        - context-management
      custom: []
  quick:
    description: Fast, focused development without spec-driven workflow overhead
    model: sonnet
    inject_skills: true
    rules:
      standard:
        - quick
        - git-operations
        - coding-standards
        - mcp-tools
        - project-conventions
        - error-handling
        - validation
        - execution-verification
        - context-management
      custom: []
  remember:
    description: Store learnings before clearing context
    model: sonnet
    rules:
      standard:
        - remember
      custom: []
